import { FilterArticleByApiPipe } from './filter-article-by-api.pipe';

describe('FilterArticleByApiPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArticleByApiPipe();
    expect(pipe).toBeTruthy();
  });
});
